export class Ingredient {
  //esto es una forma abreviada de definir el modelo, lo que recibe como parametro en el constructor son los atributos del modelo
  constructor(public name: string, public amount: number) {}
}
